<!DOCTYPE html>
<html lang="en">
<head>
    <title>Formulario de Docente</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #edf5f8;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #03A9F4;
            color: #fff;
            padding: 1em;
            text-align: center;
        }

        .btn-back {
            background-color: #03A9F4;
            color: #fff;
            border: none;
            padding: 10px 20px;
            margin: 20px 0;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }

        .btn-back:hover {
            background-color: #555;
        }

        section {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        footer {
            background-color: #333;
            color: #fff;
            padding: 1em;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>

<header>
    <h1>Formulario de Docente</h1>
</header>

<div class="container mt-3">
    <a href="/docentes" class="btn-back">Volver a Docentes</a>
    <section>
        <h2>Nuevo Docente</h2>

        <form th:action="@{/docente/form}" method="post" th:object="${docente}">
            <!-- ID -->
            <div class="mb-3 mt-3">
                <label>ID:</label>
                <input type="number" class="form-control" placeholder="ID" th:field="*{id_docente}" readonly="readonly">
            </div>

            <!-- Nombre -->
            <div class="mb-3">
                <label>Nombre:</label>
                <input type="text" class="form-control" placeholder="Ingrese el nombre" th:field="*{docente_nombre}">
            </div>

            <!-- Apellido -->
            <div class="mb-3">
                <label>Apellido:</label>
                <input type="text" class="form-control" placeholder="Ingrese el apellido" th:field="*{docente_apellido}">
            </div>

            <!-- Correo -->
            <div class="mb-3">
                <label>Correo:</label>
                <input type="email" class="form-control" placeholder="Ingrese el correo electrónico" th:field="*{correo}">
            </div>

            <!-- Especialidad -->
            <div class="mb-3">
                <label>Especialidad:</label>
                <input type="text" class="form-control" placeholder="Ingrese la especialidad" th:field="*{especialidad}">
            </div>

            <button type="submit" class="btn btn-primary">Guardar</button>
        </form>
    </section>
</div>



</body>
</html>
